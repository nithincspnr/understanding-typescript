// assign types
let myRealAge: number;

// myRealAge = '25'; // Type Error

// boolean
let hasHobbies: boolean = false;
// hasHobbies = 1;

// array
let hobbies: any[] = ["cooking", "sports"];

// Tuples
let myTuples: [number, string] = [1, "hello"];

console.log(myTuples);

// enum
enum Color {
  Gray,
  Green,
  Blue,
}

let myColor: Color = Color.Blue;
// console.log(Color, "A");
